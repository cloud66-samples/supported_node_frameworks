version: '2'
services:
  loopback:
    build:
      context: loopback/.
      dockerfile: Dockerfile
    command: npm start
    ports:
      - "3000:3000"
    volumes:
      - ${PWD}/loopback:/app
    environment:
      - PORT=3000
